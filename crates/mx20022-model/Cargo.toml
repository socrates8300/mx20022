[package]
name = "mx20022-model"
version = "0.3.0"
description = "Strongly-typed ISO 20022 message models generated from official XSD schemas (pacs, pain, camt, head)"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation = "https://docs.rs/mx20022-model"
keywords = ["iso20022", "payments", "finance", "serde", "xml"]
categories = ["encoding", "data-structures"]

[features]
default = ["pacs"]
pacs = []
# Older pacs message versions kept for back-compat with banks that have not
# upgraded. Implies `pacs`.
legacy-pacs = ["pacs"]
pain = []
camt = []
head = []
all = ["pacs", "legacy-pacs", "pain", "camt", "head"]

[dependencies]
serde.workspace = true

[lints]
workspace = true
